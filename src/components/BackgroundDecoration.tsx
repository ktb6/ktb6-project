import React from 'react';

const BackgroundDecoration = () => {
  return (
    <div className="absolute inset-0 w-screen min-h-screen first-screen-gradient dark:first-screen-gradient -z-10 mask-gradient dark:mask-gradient" />
  );
};

export default BackgroundDecoration;
